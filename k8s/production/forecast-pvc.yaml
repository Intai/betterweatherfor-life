apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: forecast-claude-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: do-block-storage
  resources:
    requests:
      storage: 1Gi
