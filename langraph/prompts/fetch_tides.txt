When are the tide turning times according to https://api.niwa.co.nz/tides/data.csv?lat={latitude}&long={longitude}&startDate={date}&numberOfDays=10&datum=LAT

Respond with raw JSON only. Do not wrap in markdown code fences or backticks. Use exactly this shape, one row per turning point:

{{"fields":["time","type","heightM"],"rows":[["2026-08-04T04:12","Low",0.42],["2026-08-04T10:30","High",3.18]]}}

NIWA reports in UTC. Convert every time to the local timezone of the geolocation ({timezone}) and emit it as `YYYY-MM-DDTHH:MM` — no seconds, no `Z`, no offset. Scoring interpolates tide height against local hourly data, so an unconverted time shifts every tide by the whole UTC offset without looking wrong.

That conversion moves the range's edges too. The CSV's opening rows carry the *previous* UTC date and still belong to the first local day: 13:46Z the day before {date} is 01:46 local on {date} itself. Keep or drop a row by its converted local time, never by the UTC date the CSV prints, or the first local day quietly loses its early turning points.

Include every turning point across all 10 days, including those outside daylight hours — interpolating tide height needs the turning points on both sides of a time window. Every row must have exactly three values in the order given: the timestamp, then `type` as a quoted High or Low, then `heightM` as a bare number.

Emit each turning point exactly once — no `time` may appear twice. If a row comes out wrong, do not append a corrected copy of it: scoring pairs your rows against a reference by position, so one extra row misaligns every row after it and fails the entire series, where a single wrong row would only have failed itself.

Use the curl tool with header `"accept": "text/csv"` and referrer `https://tides.niwa.co.nz/`.
